@model Task1.Models.DataVM

<h2>編輯頁面</h2>

@using (Html.BeginForm("Access", "Data", new { AccessType = "Update", Id = Model.Id }, FormMethod.Post))
{
    <div class="form-group">
        <div class="form-group">
            <label>名字</label>
            <input class="form-control" id="Context" name="Context" value="@Model.Context">
        </div>
        <button type="submit" class="btn btn-primary">確定</button>
        <a class="btn btn-success" href="@Url.Action("Index", "Data")">取消</a>
    </div>
}